spring:
  application:
    name: "demo"
  output:
    ansi:
      enabled: ALWAYS
  datasource:
    url: jdbc:mariadb://localhost/test
    username: taltest
    password: 1234
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true
